# SchemaSentry Report

Generated at: 2026-03-03T11:02:11.778Z

## Summary

- P0: 3
- P1: 4
- P2: 6
- Total findings: 13

## Findings

### 1. [P0] Float type used for money-like column

- Rule ID: `P0_FLOAT_FOR_MONEY`
- Description: Floating-point types can introduce rounding errors in financial calculations.
- Suggested fix: ALTER TABLE payments ALTER COLUMN <column> TYPE numeric(18,2);
- Evidence:
  - payments.amount double precision

### 2. [P0] Table has no primary key

- Rule ID: `P0_NO_PRIMARY_KEY`
- Description: Tables without a primary key risk duplicates and unsafe updates.
- Suggested fix: ALTER TABLE payment_events ADD COLUMN id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY;
- Evidence:
  - table payment_events

### 3. [P0] Timestamp-like column stored as text/varchar

- Rule ID: `P0_TEXT_FOR_TIMESTAMP`
- Description: Text timestamps can break ordering, validation, and query performance.
- Suggested fix: ALTER TABLE payments ALTER COLUMN <column> TYPE timestamptz USING <column>::timestamptz;
- Evidence:
  - payments.created_at varchar(64)

### 4. [P1] Missing index on *_id column

- Rule ID: `P1_MISSING_INDEX_ON_ID_COLUMN`
- Description: Foreign-key-style columns without indexes can cause slow joins and lock-heavy parent deletes.
- Suggested fix: CREATE INDEX CONCURRENTLY idx_payments_<column> ON payments (<column>);
- Evidence:
  - payments.customer_id

### 5. [P1] Missing index on *_id column

- Rule ID: `P1_MISSING_INDEX_ON_ID_COLUMN`
- Description: Foreign-key-style columns without indexes can cause slow joins and lock-heavy parent deletes.
- Suggested fix: CREATE INDEX CONCURRENTLY idx_payment_events_<column> ON payment_events (<column>);
- Evidence:
  - payment_events.payment_id

### 6. [P1] Nullable *_id column

- Rule ID: `P1_NULLABLE_ID_COLUMN`
- Description: Nullable identifier/foreign key columns can lead to orphaned and inconsistent records.
- Suggested fix: ALTER TABLE payments ALTER COLUMN <column> SET NOT NULL;
- Evidence:
  - payments.customer_id

### 7. [P1] Nullable *_id column

- Rule ID: `P1_NULLABLE_ID_COLUMN`
- Description: Nullable identifier/foreign key columns can lead to orphaned and inconsistent records.
- Suggested fix: ALTER TABLE payment_events ALTER COLUMN <column> SET NOT NULL;
- Evidence:
  - payment_events.payment_id

### 8. [P2] ENUM type usage detected

- Rule ID: `P2_ENUM_USAGE`
- Description: ENUM changes can add migration friction for frequently changing business states.
- Suggested fix: Consider lookup tables + foreign keys (or controlled smallint mapping) for rapidly changing states.
- Evidence:
  - payment_status values: pending, paid, failed, refunded

### 9. [P2] JSON/JSONB column present

- Rule ID: `P2_JSON_USAGE`
- Description: JSON fields are flexible but can become expensive to query and index if overused.
- Suggested fix: For hot JSON paths, extract typed columns and/or add targeted GIN indexes.
- Evidence:
  - payments.meta jsonb

### 10. [P2] JSON/JSONB column present

- Rule ID: `P2_JSON_USAGE`
- Description: JSON fields are flexible but can become expensive to query and index if overused.
- Suggested fix: For hot JSON paths, extract typed columns and/or add targeted GIN indexes.
- Evidence:
  - payment_events.payload jsonb

### 11. [P2] SERIAL family used instead of IDENTITY

- Rule ID: `P2_SERIAL_USAGE`
- Description: IDENTITY columns are the modern Postgres standard and reduce sequence ownership pitfalls.
- Suggested fix: Prefer `GENERATED BY DEFAULT AS IDENTITY` for new columns/tables.
- Evidence:
  - customers.id bigserial

### 12. [P2] SERIAL family used instead of IDENTITY

- Rule ID: `P2_SERIAL_USAGE`
- Description: IDENTITY columns are the modern Postgres standard and reduce sequence ownership pitfalls.
- Suggested fix: Prefer `GENERATED BY DEFAULT AS IDENTITY` for new columns/tables.
- Evidence:
  - payments.id bigserial

### 13. [P2] SERIAL family used instead of IDENTITY

- Rule ID: `P2_SERIAL_USAGE`
- Description: IDENTITY columns are the modern Postgres standard and reduce sequence ownership pitfalls.
- Suggested fix: Prefer `GENERATED BY DEFAULT AS IDENTITY` for new columns/tables.
- Evidence:
  - payment_events.id bigserial
