# SchemaSentry Report

Generated at: 2026-03-03T11:02:11.746Z

## Summary

- P0: 1
- P1: 3
- P2: 4
- Total findings: 8

## Findings

### 1. [P0] Table has no primary key

- Rule ID: `P0_NO_PRIMARY_KEY`
- Description: Tables without a primary key risk duplicates and unsafe updates.
- Suggested fix: ALTER TABLE audit_log ADD COLUMN id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY;
- Evidence:
  - table audit_log

### 2. [P1] Missing index on *_id column

- Rule ID: `P1_MISSING_INDEX_ON_ID_COLUMN`
- Description: Foreign-key-style columns without indexes can cause slow joins and lock-heavy parent deletes.
- Suggested fix: CREATE INDEX CONCURRENTLY idx_users_<column> ON users (<column>);
- Evidence:
  - users.tenant_id

### 3. [P1] Missing index on *_id column

- Rule ID: `P1_MISSING_INDEX_ON_ID_COLUMN`
- Description: Foreign-key-style columns without indexes can cause slow joins and lock-heavy parent deletes.
- Suggested fix: CREATE INDEX CONCURRENTLY idx_audit_log_<column> ON audit_log (<column>);
- Evidence:
  - audit_log.tenant_id
  - audit_log.actor_user_id

### 4. [P1] Nullable *_id column

- Rule ID: `P1_NULLABLE_ID_COLUMN`
- Description: Nullable identifier/foreign key columns can lead to orphaned and inconsistent records.
- Suggested fix: ALTER TABLE audit_log ALTER COLUMN <column> SET NOT NULL;
- Evidence:
  - audit_log.tenant_id
  - audit_log.actor_user_id

### 5. [P2] JSON/JSONB column present

- Rule ID: `P2_JSON_USAGE`
- Description: JSON fields are flexible but can become expensive to query and index if overused.
- Suggested fix: For hot JSON paths, extract typed columns and/or add targeted GIN indexes.
- Evidence:
  - audit_log.context jsonb

### 6. [P2] SERIAL family used instead of IDENTITY

- Rule ID: `P2_SERIAL_USAGE`
- Description: IDENTITY columns are the modern Postgres standard and reduce sequence ownership pitfalls.
- Suggested fix: Prefer `GENERATED BY DEFAULT AS IDENTITY` for new columns/tables.
- Evidence:
  - tenants.id bigserial

### 7. [P2] SERIAL family used instead of IDENTITY

- Rule ID: `P2_SERIAL_USAGE`
- Description: IDENTITY columns are the modern Postgres standard and reduce sequence ownership pitfalls.
- Suggested fix: Prefer `GENERATED BY DEFAULT AS IDENTITY` for new columns/tables.
- Evidence:
  - users.id bigserial

### 8. [P2] SERIAL family used instead of IDENTITY

- Rule ID: `P2_SERIAL_USAGE`
- Description: IDENTITY columns are the modern Postgres standard and reduce sequence ownership pitfalls.
- Suggested fix: Prefer `GENERATED BY DEFAULT AS IDENTITY` for new columns/tables.
- Evidence:
  - audit_log.id bigserial
